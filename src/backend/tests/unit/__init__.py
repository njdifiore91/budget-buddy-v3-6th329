"""
Unit tests package for the Budget Management Application.

This package contains unit tests for individual components of the Budget Management
Application. The test modules are organized to correspond with the application's
components, making it easier to locate and run specific tests.
"""

import os.path

# Path to the unit tests directory for easy access in test modules
UNIT_TESTS_DIR = os.path.dirname(os.path.abspath(__file__))